
Update:

We need to implement two additional data drift checks. While reviewing and understanding the script shared by Seema, we identified an issue in our current approach.

The profile.json we have implemented currently is only for the target retailer. As per the script provided in Seema’s document, we need to generate a separate profile.json for each retailer using that script. However, we are currently using the same profile across all datasets, which makes our drift detection rely on a static reference and results in alerts being triggered for almost every dataset.

To address this properly, we need to make the logic more dynamic by calculating historical statistics for each retailer individually .
